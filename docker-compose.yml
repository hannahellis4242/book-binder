version: "3"
services:
  page_sequence:
    image: ghcr.io/hannahellis4242/bpss/book-page-sequence-service:latest
    restart: always
  web:
    build: .
    expose:
      - 80
    ports:
      - 80:8080
    restart: always